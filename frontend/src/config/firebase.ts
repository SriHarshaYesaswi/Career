// Firebase SDK removed — this project uses pure MERN stack auth (bcrypt + JWT via Express).
// Authentication is handled by /api/auth endpoints on the backend.
// This file is intentionally empty.
export {};
